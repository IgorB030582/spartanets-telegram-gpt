# spartanets-telegram-bot

A minimal Telegram bot powered by `python-telegram-bot`.

## How to use

1. Set up `.env` with your bot token
2. Install requirements:
```
pip install -r requirements.txt
```
3. Run it:
```
python main.py
```